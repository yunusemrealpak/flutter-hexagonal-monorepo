# Created last and never added to the root workspace: list.
name: billing_testing
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  billing_api: ^0.1.0
  core_kernel: ^0.1.0
  core_ports: ^0.1.0

dev_dependencies:
  test: ^1.26.0

