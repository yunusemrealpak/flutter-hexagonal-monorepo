import 'package:meta/meta.dart';

/// The import above is a dev dependency, so it must not appear under lib/.
@immutable
final class CollectPayment {
  /// Creates the use case.
  const CollectPayment();
}
