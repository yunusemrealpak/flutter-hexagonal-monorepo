/// A closed union, the shape freezed is actually good at.
///
/// The generated half sits beside it in `shipment_status.freezed.dart` and is
/// what proves S8's suffix heuristic stays quiet in output nobody named.
sealed class ShipmentStatus {
  const ShipmentStatus();
}
