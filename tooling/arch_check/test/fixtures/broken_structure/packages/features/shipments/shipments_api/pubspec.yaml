# S5: the pubspec calls itself shipments_contract while the directory is
# shipments_api. S6: the package never declares resolution: workspace, even
# though the root does list its path.
name: shipments_contract
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.0

dependencies:
  core_kernel: ^0.1.0
