import 'package:go_router/go_router.dart';

/// Declares its destinations in the router library the app happens to use.
final class StopRoutes {
  /// The one destination.
  const StopRoutes();

  /// What the app would mount.
  GoRoute get route => const GoRoute();
}
