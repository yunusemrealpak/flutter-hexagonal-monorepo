# Section 6. Generation in the innermost ring, serialization in a contract package, and an unpinned builder.
name: fixture_workspace
publish_to: none
version: 0.0.0

workspace:
  - packages/core/core_kernel
  - packages/features/shipments/shipments_api
  - packages/features/shipments/shipments_infrastructure

environment:
  sdk: ^3.12.0
