/// The sync use cases. Pure Dart, and blind to every adapter that answers its
/// ports.
///
/// Everything this package publishes is exported here and nowhere else.
/// Another package importing `package:sync_application/src/...` is reaching
/// across a boundary, and arch_check reports it as one.
library;

// Nothing yet: the use cases arrive in the next commit. A barrel with no
// export is a package with no public surface, which is exactly what this one
// has until then.
