name: core_navigation
description: >-
  Route contracts shared by presentation packages and assembled by an app.
  Pure Dart: it describes destinations, it does not render them.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  core_kernel: ^0.1.0

dev_dependencies:
  test: ^1.26.0
